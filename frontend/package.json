{
	"name": "fullstack-weather-frontend",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"start": "react-scripts start",
		"build": "react-scripts build",
		"heroku-postbuild": "cd frontend/ && npm install --only=dev && NODE_ENV=development npm install --no-shrinkwrap && npm run build"
	},
	"author": "Rafael Avilés",
	"license": "MIT",
	"dependencies": {
		"react": "^17.0.1",
		"react-dom": "^17.0.1",
		"react-scripts": "^4.0.3",
		"web-vitals": "^1.1.0"
	},
	"eslintConfig": {
		"extends": "react-app"
	},
	"browserslist": {
		"production": [
			">0.2%",
			"not dead",
			"not op_mini all"
		],
		"development": [
			"last 1 chrome version",
			"last 1 firefox version",
			"last 1 safari version"
		]
	},
	"devDependencies": {
		"@types/react": "^17.0.2",
		"@types/react-dom": "^17.0.1",
		"typescript": "^4.1.5"
	},
	"proxy": "http://localhost:5000"
}
